---

---

<header
    class="fixed top-0 w-full z-50 px-6 py-6 md:px-12 flex justify-between items-start mix-blend-multiply pointer-events-none"
>
    <!-- Volume Indicator -->
    <div
        class="hidden md:block w-24 text-xs font-bold tracking-widest uppercase opacity-40 text-ink"
    >
        Vol. IV
    </div>

    <!-- Menu Button -->
    <div class="pointer-events-auto ml-auto">
        <button
            class="flex items-center justify-center gap-2 h-10 px-6 bg-primary text-white rounded hover:bg-opacity-90 transition-all shadow-sm"
        >
            <span class="text-sm font-bold tracking-wide uppercase">Menu</span>
            <span class="material-symbols-outlined text-[18px]">menu</span>
        </button>
    </div>
</header>
